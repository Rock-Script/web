import { createTheme } from '@mui/material/styles';

const theme = createTheme({

  palette: {
    primary: {
      main: '#ff0000',
      contrastText: '#ffffff',
    },
    neutral: {
      main: '#FF0000',
      contrastText: '#fff',
    },
  },
});

export default theme;